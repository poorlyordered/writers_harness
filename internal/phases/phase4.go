package phases

import (
	"context"
	"fmt"
)

// RunPhase4 is a placeholder for the Phase 4 (Prose Generation) session flow.
// Implementation follows in the next build iteration (SPEC-006).
func RunPhase4(_ context.Context, _ Phase1Config) error {
	return fmt.Errorf("Phase 4 not yet implemented — see SPEC-006")
}
