package phases

import (
	"context"
	"fmt"
)

// RunPhase2 is a placeholder for the Phase 2 (Idea Expansion) session flow.
// Implementation follows in the next build iteration (SPEC-003).
func RunPhase2(_ context.Context, _ Phase1Config) error {
	return fmt.Errorf("Phase 2 not yet implemented — see SPEC-003")
}
